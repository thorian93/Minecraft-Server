[
{% for user in minecraft_users_ops %}
  {
    "uuid": "{{ user.uuid }}",
    "name": "{{ user.name }}",
    "level": "{{ user.level }}",
    "bypassesPlayerLimit": "{{ user.bypassplayerlimit }}"
  }
{% endfor %}
]
